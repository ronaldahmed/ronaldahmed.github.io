/*
#include <cstdio>
#include <iostream>
#include <cstring>

using namespace std;
int memo[1000];
int dp(int A){
	if(memo[A]!=-1)	return memo[A];
	if(A==0)				return 0;
	
}

int main(){
	int t,a,b,k;
	cin>>t;
	while(t--){
		cin>>a>>b>>k;
		memset(memo,-1,sizeof memo);
		
		printf("%d\n",dp(b).dp(a-1));
	}
	return 0;
}
*/
